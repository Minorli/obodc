# third_party

Canonical home for vendored source and local binary dependencies.

Current state:

- `third_party/binaries/` now owns locally installed binary dependencies that previously lived under `import/`

Planned mappings:

- `third_party/source` for vendored source code
- `third_party/binaries` for local binary/runtime dependencies
